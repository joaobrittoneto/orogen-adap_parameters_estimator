# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(adap_parameters_estimatorTaskLib)
ADD_LIBRARY(${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_NAME} SHARED 
    ${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_SOURCES})
add_dependencies(${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ADAP_PARAMETERS_ESTIMATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/adap_parameters_estimator)

